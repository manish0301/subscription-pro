{
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "backend/subscription-api/Dockerfile"
  },
  "deploy": {
    "startCommand": "gunicorn --bind 0.0.0.0:$PORT main:app",
    "healthcheckPath": "/health"
  }
}